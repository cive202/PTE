## Final Implementation Plan: Indian & US ARPA Focus

As confirmed, I will now:
1.  **Simplify [validator.py](file:///c:/Users/Acer/DataScience/PTE/api/validator.py)**: Remove Nigerian, US_MFA, and UK accent configurations.
2.  **Implement Parallelism**: Update the `align_and_validate_gen` function to run the Indian and US_ARPA Docker alignments concurrently using `concurrent.futures.ThreadPoolExecutor`.
3.  **Update Anchor Logic**: Set `US_ARPA` as the primary reference for the word-level TextGrid results.
4.  **Refactor for Speed**: Move the Docker command execution into a dedicated concurrent-friendly helper function.

**Starting the implementation now.**
